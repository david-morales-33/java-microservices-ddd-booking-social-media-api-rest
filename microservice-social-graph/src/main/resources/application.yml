server:
  port: 8090
spring:
  application:
    name: msvc-social-graph
  config:
    import: optional:configserver:http://localhost:8888
eureka:
  instance:
    hostname: localhost
  client:
    server-url:
      defaultZone: http://localhost:${server.port}/eureka/